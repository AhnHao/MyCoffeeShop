<nav class="navbar navbar-expand-lg navbar_config">
    <div class="container">
      <a class="navbar-brand" href="/">
        <img src="/images/logo.png" alt="Logo" class="object-fit-cover img-logo">
      </a>
      <button class="navbar-toggler text-white btn btn-lg border border-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <i class="bi bi-list"></i>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item border-end">
            <a class="nav-link px-4 <%= path === '/' ? 'text-warning' : 'text-white' %>" href="/">Home</a>
          </li>
          <li class="nav-item border-end">
            <a class="nav-link px-4 <%= path === '/menu' ? 'text-warning' : 'text-white' %>" href="/menu">Menu</a>
          </li>
          <% if(isAuthenticated) {%>
            <li class="nav-item border-end">
              <a class="nav-link px-4 <%= path === '/cart' ? 'text-warning' : 'text-white' %>" href="/cart">Cart</a>
            </li>
            <li class="nav-item border-end">
              <a class="nav-link px-4 <%= path === '/orders' ? 'text-warning' : 'text-white' %>" href="/orders">Orders</a>
            </li>
            <li class="nav-item border-end">
              <a class="nav-link px-4 <%= path === '/admin/add-product' ? 'text-warning' : 'text-white' %>" href="/admin/add-product">Add Product</a>
            </li>
            <li class="nav-item border-end">
              <a class="nav-link px-4 <%= path === '/admin/products' ? 'text-warning' : 'text-white' %>" href="/admin/products">Admin Product</a>
            </li>
          <% } %>
        </ul>
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <% if(!isAuthenticated) {%>
            <li class="nav-item border-end">
                <a class="nav-link px-4 <%= path === '/login' ? 'text-warning' : 'text-white' %>" href="/login">Login</a>
            </li>
            <li class="nav-item border-end">
                <a class="nav-link px-4 <%= path === '/signup' ? 'text-warning' : 'text-white' %>" href="/signup">Sign Up</a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <form action="/logout" method="POST">
                <button class="btn text-white border" type="submit">Logout</button>
              </form>
          </li>
          <% } %>
        </ul>
      </div>
    </div>
</nav>